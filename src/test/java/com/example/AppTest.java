package com.example;

import org.junit.jupiter.api.Test;

class AppTest {

    @Test
    void testMain() {
        // Execute main method to verify it runs without crashing (exceptions are caught
        // internally)
        App.main(new String[] {});
    }
}
